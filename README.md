# easy-codeshare

A code editor that lets a user create a room and code alongside someone else over the web. Also lets them whiteboard on the same page.

# Judge0

The app makes use of a self-hosted Judge0 cluster. To run it, cd into /judge0 and run docker compose up -d
